/// \file ffi_cos.mla
/// @ingroup examples
/// \brief FFI demo calling libm cos() from MLang.
///
extern fn cos(x : f64) -> f64;

fn main() -> i32
{
    let v : f64 = cos(0.0);
    println!("cos(0) = {}", v);
    return 0;
}
