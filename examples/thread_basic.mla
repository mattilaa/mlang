fn worker() -> void {
    println!("worker started");
}

fn main() -> int {
    let handle: Handle<Thread> = thread_spawn(worker);
    let status: int = thread_join(handle);
    println!("join status = {}", status);
    return 0;
}
