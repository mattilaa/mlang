/// \file inspect.mla
/// @ingroup examples
/// \brief Package-manager multi-binaries demo: inspect entry point.
///
mod zlib;

use zlib::*;

fn main() -> i32 {
    let version: str8 = zlibVersion();
    println!("inspect target built with tar.gz zlib={}", version);
    return 0;
}
