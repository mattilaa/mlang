#[derive(Debug)]
struct Point { var x: i32; var y: i32; };

fn main() -> i32 {
    let origin: Point = Point { x: 0, y: 0 };
    println!("{origin}");
    println!(origin); // also works now
    return 0;
}
