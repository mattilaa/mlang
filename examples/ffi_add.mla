/// \file ffi_add.mla
/// @ingroup examples
/// \brief FFI example calling a small C add() function.
///
extern fn c_add(a: i32, b: i32) -> i32;

fn main() -> i32 {
    let sum: i32 = c_add(2, 40);
    println!("sum: {}", sum);
    return 0;
}
