struct SomeStruct : BaseStruct {
    let a: int = 1;

    /*
      Ignore this member
    var b: float;
    */

    var c: int;
};

// Comments work here

struct OtherStruct {
    let a: int = 1; // Just some variable
    var b: float; // No need to init mutable variables
};

fn calculate(a: int, b: float) -> int {
    var result: int = 1;
    SomeStruct st;
    let d: double = 2.0;
    let str: string = "foo";
    let l: list = [1,2,3,4];
    result = a + int(b);
    return result;
}

fn main() -> void {
    let x: int = 1;
    var y: float = 0.0;
    x = 5;
    y = 3.14;
    let y: int = calculate(x, y);
    return;
}

fn max(a: int, b: int) -> int {
    if a > b: {
        a = b;
        return a;
    }
    if a > b: return a;
    else if b > a: return b;
    else: return a;
    return a;
}

