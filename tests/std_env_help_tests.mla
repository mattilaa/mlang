mod std::env;
mod std::strbuf;
mod std::testing;

use std::env::arg_or;
use std::env::get;
use std::env::wants_help;
use std::strbuf::len;
use std::testing::verify_eq;
use std::testing::verify;

#[test]
fn test_env_wants_help_and_arg_or() {
    var args_help: list<str8> = [];
    args_help.push("demo");
    args_help.push("--help");
    verify_eq(1, wants_help(args_help));

    var args0: list<str8> = [];
    args0.push("demo");
    args0.push("--help");
    verify_eq("demo", arg_or(args0, 0, "fallback"));

    var args9: list<str8> = [];
    args9.push("demo");
    args9.push("--help");
    verify_eq("fallback", arg_or(args9, 9, "fallback"));
}

#[test]
fn test_env_get_path() {
    verify(len(get("PATH")) > 0);
}
