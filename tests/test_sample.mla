mod std::testing;

use std::testing::verify;
// Sample tests for `mlang test`

#[test]
fn test_addition() {
    let x: i32 = 2 + 2;
    verify(x == 4);
}

#[test]
fn test_result_ok() {
    let r: Result<i32, string> = Ok<i32, string>(123);
    verify(r.is_ok());
}
