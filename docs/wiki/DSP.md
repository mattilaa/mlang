<!-- Generated by scripts/generate_github_wiki.py. Update the source documentation and rerun the script. The generator adds wiki links and code-fence languages. -->

# DSP Library

Module file: `modules/dsp/mod.mla`

Digital signal processing namespace root.

Submodules:
- [dsp::convolution](DSP-Convolution)
- [dsp::filter](DSP-Filter)
- [dsp::fft](DSP-FFT)

Import specific DSP modules such as [`dsp::convolution`](DSP-Convolution), [`dsp::filter`](DSP-Filter), and
[`dsp::fft`](DSP-FFT) in application code. This library is installed and discovered
separately from the `std` namespace.

The compiler searches the repository's `modules/` directory in source builds,
then user and system module locations. Set `MLANG_MODULE_PATH` to use a custom
module root.
